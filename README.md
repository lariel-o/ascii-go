# A package to print an ASCII table with go

The usage is very simple, just import the repo with git clone ```https://github.com/lariel-o/ascii-go``` an run with ``go run .``. Otherside you can also make a bin file with ``go build .`` and execute it with your shell.

You also can do ``go run . -h`` to see how to use arguments
